# TestVibe Templates

Cookbook templates for custom extensions shown in ExtensionsView.

## Providers
- `Simple`
- `SQL`
- `MySQL`
- `REST`

Each provider folder contains a `tools.js` module that follows the metadata and export patterns consumed by TestVibe.
